# RimTavern 集中测试脚本（TEST 素材）

> 素材：`Cards/TEST_*.chara.json`（主角卡 1 + 配角卡 2）、`WorldBooks/TEST_JadeHarbor.worldinfo.json`。
> 目标：验证 ① 角色卡影响台词/选项；② 世界书常开 + 关键词命中注入；③ 不再复读开场白。

## 准备（游戏内，1 分钟）
1. 进存档后：Mod 设置 → RimTavern → 点“重新加载内容文件”（日志应显示：角色卡 3+ 张 / 世界书条目 4 条）。
2. 章节状态栏填：章节标题=`翡翠港疑云`；当前目标/氛围=`有人在打听失踪舰队旧事，主角想借机找导师下落。`
3. [分配]：主角卡 TEST艾拉 → 你的主角；TEST卡尔 → 一名殖民者；TEST小夜 → 另一名殖民者（或用调试按钮让它们来搭话）。

## 会话 1：验证角色卡 + 世界书常开
- 以主角右键【卡尔】开对话。
- 期望：
  - NPC 开场应自然、不机械（卡尔短句 + 机械比喻，不该复读同一句）；
  - `req|` 的 system 里同时出现：【当前章节】、世界基调常开条目（出现“失踪舰队”）、卡尔卡与艾拉卡；
  - 主角选项应带艾拉的“冷峻/旧约动机”色彩（而不是纯“太空军医职业腔”）。

## 会话 2：验证关键词命中（信标 / 翡翠港 / 旧约）
- 主角自由输入（点“发送”前用自由输入框）：`我带着一枚旧信标，信号站的事你还记得多少？`
- 期望：`lore|hit 命中条目：信标`，且 NPC 回应里能自然提到“异常信号/被抹除的记录”而不编造新设定。
- 再输入：`翡翠港那边，有人卖过旧约碎片？` → 期望 `lore|hit` 命中“翡翠港”“旧约”（含英文别名如 beacon/jade harbor 也能命中）。
- 若 `lore|miss`：对照该段的“搜索样本”，确认关键词真的出现在样本里（检查输入是否含中文词或英文别名）。

## 会话 3：验证 P4 长线记忆 + 隐式好感（需重开对话）
- 在对话里让主角答应/威胁 NPC 一件事（如“我会带旧信标来找你”），**正常点“结束对话”关闭**；
- 期望：关闭后片刻，`Diag` 出现 `mem|outcome`（好感变化与摘要）；日志显示记忆已入库；
- **再次右键同一 NPC 开新对话**：`req|` 中应出现【此前记忆】并带上上次的摘要/承诺 → NPC 应能记得并接话（跨会话，不再只是窗口级）；
- 好感影响：设置页暂无展示；可在日志 `mem|outcome` 看 好感 old→new。

## 复读检查
- 连续对话 ≥6 轮：NPC 与主角台词都不应整句重复（规则6 + 开场语只在第一轮）。
- 若仍复读：复制调试日志发回，重点看 `req|npc-turn` 第二轮的“当前场景”是否仍写成“由对方先开口”。

## 判定清单
- [ ] 设置页计数正确（卡3 / 书4）
- [ ] 常开条目出现在每次 `req|`（世界基调）
- [ ] 关键词条目出现 `lore|hit` 且内容进入 `req|`
- [ ] 结束对话后出现 `mem|outcome`（好感 + 摘要）
- [ ] 重开同人对话出现【此前记忆】且 NPC 记得上回约定
- [ ] 章节状态【当前章节】出现在 NPC 回合 system
- [ ] 无复读开场
- [ ] 无 `<color=…>` 富文本垃圾
- [ ] 主角选项带有艾拉卡色彩
- [ ] 无复读开场
- [ ] 无 `<color=…>` 等富文本垃圾
